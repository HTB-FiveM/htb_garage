function RunStartupStuffNone()
    -- Need to build a way to handle player loaded (not connecting) without a framework
    --
    -- AddEventHandler('esx:playerLoaded', function()
    --     TriggerServerEvent('htb_garage:playerLoaded')
    -- end)

end
